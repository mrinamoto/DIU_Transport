const express=require('express');const{authorize}=require('../middleware/authorize');
function createFeedbackRouter({authenticate,feedbackService,feedbackLimiter}){const router=express.Router();router.use(authenticate);router.post('/',feedbackLimiter,(req,res,next)=>{try{res.status(201).json({status:'success',data:feedbackService.submit(req.body||{},req.user.id)});}catch(e){next(e);}});router.get('/mine',(req,res,next)=>{try{const data=feedbackService.mine(req.user.id);res.json({status:'success',count:data.length,data});}catch(e){next(e);}});router.get('/mine/:id',(req,res,next)=>{try{res.json({status:'success',data:feedbackService.getMine(Number(req.params.id),req.user.id)});}catch(e){next(e);}});return router;}
function createAdminFeedbackRouter({authenticate,feedbackService}){const router=express.Router();router.use(authenticate,authorize('ADMIN'));router.get('/',(req,res,next)=>{try{res.json({status:'success',...feedbackService.adminList(req.query)});}catch(e){next(e);}});router.get('/:id',(req,res,next)=>{try{res.json({status:'success',data:feedbackService.adminGet(Number(req.params.id))});}catch(e){next(e);}});router.patch('/:id',(req,res,next)=>{try{res.json({status:'success',data:feedbackService.update(Number(req.params.id),req.body||{},{actorUserId:req.user.id,requestId:req.id})});}catch(e){next(e);}});return router;}
module.exports={createFeedbackRouter,createAdminFeedbackRouter};
