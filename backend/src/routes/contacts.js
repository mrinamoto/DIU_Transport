const express=require('express');const{authorize}=require('../middleware/authorize');
function createContactRouter({authenticate,contactService}){const router=express.Router();router.use(authenticate);router.get('/',(req,res,next)=>{try{const data=contactService.list({isAdmin:req.user.role==='ADMIN'});res.json({status:'success',count:data.length,data});}catch(e){next(e);}});router.get('/:id',(req,res,next)=>{try{res.json({status:'success',data:contactService.getById(Number(req.params.id),{isAdmin:req.user.role==='ADMIN'})});}catch(e){next(e);}});router.post('/',authorize('ADMIN'),(req,res,next)=>{try{res.status(201).json({status:'success',data:contactService.create(req.body||{},{actorUserId:req.user.id,requestId:req.id})});}catch(e){next(e);}});router.patch('/:id',authorize('ADMIN'),(req,res,next)=>{try{res.json({status:'success',data:contactService.update(Number(req.params.id),req.body||{},{actorUserId:req.user.id,requestId:req.id})});}catch(e){next(e);}});router.delete('/:id',authorize('ADMIN'),(req,res,next)=>{try{res.json({status:'success',data:contactService.deactivate(Number(req.params.id),{actorUserId:req.user.id,requestId:req.id})});}catch(e){next(e);}});router.post('/:id/reactivate',authorize('ADMIN'),(req,res,next)=>{try{res.json({status:'success',data:contactService.reactivate(Number(req.params.id),{actorUserId:req.user.id,requestId:req.id})});}catch(e){next(e);}});return router;}
module.exports={createContactRouter};
